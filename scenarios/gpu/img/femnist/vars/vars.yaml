# Scenario
dataset: 'femnist'
strategy: 'testencoding'
client: 'EncodingClient'
model: 'cnn'
compression: 'AE'
path_to_encoder_weights: '/app/artifacts/pre-trained/mnist'

# Server args
num_rounds: 1000
total_num_clients: 3550
min_available_clients: 100
min_fit_clients: 100
fraction_fit: 1
local_epochs: 1
n_clusters: 3

# Deployment
n_client_server_node: 30
n_client_per_node: 35

async: 7200
